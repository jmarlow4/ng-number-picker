import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.number.picker',
  appName: 'number-picker',
  webDir: 'dist/number-picker',
  bundledWebRuntime: false,
};

export default config;
